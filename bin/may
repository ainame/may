#!/usr/bin/env ruby
$:.unshift File.expand_path(File.join(File.dirname(__FILE__), '../lib'))
require 'may'
# require 'methadone'
# include Methadone::Main
# include Methadone::CLILogging

# version     May::VERSION
# description 'generate Objective-C files'
# arg 'path', 'file name which you want to generate'
# options['super_class'] = 'NSObject'
# on('-s SUPER_CLASS', '--super_class', 'Super Class', /.*/)
# on('-a', '--async_test', 'add TKRGuard to test cases')
# on('-m', '--mock_test',  'add OCMock to test case')

# main do |path|
#   May::Command.generate(path, options)
# end

# go!

require 'claide'

May::Command.run(ARGV)
